name: playwright_protocol
description: >-
  Protocol types, transport envelopes, errors and the event emitter shared by
  the Playwright for Dart packages. Not useful on its own.
version: 0.1.0
repository: https://github.com/insinfo/dart_playwright
issue_tracker: https://github.com/insinfo/dart_playwright/issues

topics:
  - playwright
  - browser
  - automation

environment:
  sdk: ^3.6.2

resolution: workspace

dependencies:
  meta: ^1.9.0
  collection: ^1.18.0
