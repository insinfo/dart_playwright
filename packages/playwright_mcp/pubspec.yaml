name: playwright_mcp
description: >-
  A Model Context Protocol server that drives Chromium, Firefox or WebKit
  through the Dart port of Playwright, over stdio.
version: 0.1.0
repository: https://github.com/insinfo/dart_playwright
issue_tracker: https://github.com/insinfo/dart_playwright/issues

topics:
  - mcp
  - playwright
  - browser
  - automation
  - agents

environment:
  sdk: ^3.6.2

resolution: workspace

executables:
  playwright_mcp:

dependencies:
  playwright: ^0.1.0

dev_dependencies:
  async: ^2.11.0
  test: ^1.25.0
